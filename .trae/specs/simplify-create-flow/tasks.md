# Tasks
- [x] Task 1: 验证并实现全局数据持久化
  - [x] SubTask 1.1: 检查 `useAppStore.ts` 是否已实现持久化（如使用 `zustand/middleware/persist`）。如果没有，则引入并配置本地存储，确保历史记录、项目等数据重启应用时不丢失。
- [x] Task 2: 重构“新建内容”页面布局，支持双模式（项目模式/零散模式）
  - [x] SubTask 2.1: 移除原有的 `ProjectSelector` 卡片。
  - [x] SubTask 2.2: 增加“打开本地文件夹”入口按钮。
  - [x] SubTask 2.3: 在项目模式下，实现一个简单的侧边栏或面板，展示所选本地文件夹的文件目录树。
- [x] Task 3: 升级输入框（IdeaInput），支持高级上下文引用
  - [x] SubTask 3.1: 实现拖拽本地文件到输入框，自动读取并添加为上下文（以可视化 Tag 形式显示）。
  - [x] SubTask 3.2: 实现输入框键入 `@` 唤出当前项目文件夹内的文件列表菜单，选中后添加为上下文。
- [x] Task 4: 实现结果写回本地文件夹
  - [x] SubTask 4.1: 在生成结果展示区（ResultDisplay），如果在项目模式下，增加“保存到项目”按钮，将结果直接作为 Markdown 或 TXT 文件写入该本地文件夹中。