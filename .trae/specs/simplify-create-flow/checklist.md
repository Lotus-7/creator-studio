- [x] 重启应用后，历史记录页面的数据仍然存在且未丢失。
- [x] 可以在“新建内容”页面一键打开本地文件夹，并在左侧或指定区域看到文件目录树。
- [x] 在未打开文件夹时（零散模式），依然可以正常输入并生成内容。
- [x] 拖动一个 `.txt` 或 `.md` 文件到输入框区域，能成功将其作为上下文添加。
- [x] 在项目模式下，在输入框输入 `@` 能看到项目文件列表，并能成功选中作为上下文。
- [x] 生成的内容点击“保存到项目”后，可以在本地项目文件夹中真实看到对应的文件。